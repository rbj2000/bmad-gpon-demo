# =============================================================================
# preset-small.yaml
# Generated by: demo-scope-configurator | Date: 2026-02-28
# Purpose: Small preset (~615 records) matching the current demo default.
# =============================================================================

size:
  sites: 5
  olts: 10
  splitters: 40
  onts: 160

depth:
  splitter_cascade_levels: 1
  slots_per_olt: [2, 4]    # min, max service slots

complexity:
  vendor_mode: multi        # Huawei, Nokia, ZTE
  ont_model_variety: normal # all 4 models
  failure_rate_percent: 5
