# =============================================================================
# preset-minimal.yaml
# Generated by: demo-scope-configurator | Date: 2026-02-28
# Purpose: Minimal preset (~50 records) for quick smoke tests.
# =============================================================================

size:
  sites: 1
  olts: 1
  splitters: 2
  onts: 8

depth:
  splitter_cascade_levels: 1
  slots_per_olt: [2, 2]    # min, max service slots

complexity:
  vendor_mode: single       # single = Huawei only
  primary_vendor: Huawei
  ont_model_variety: low    # 2 models
  failure_rate_percent: 0   # all ACTIVE for smoke tests
